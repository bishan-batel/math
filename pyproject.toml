[project]
name = "holomorphic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dearpygui>=2.2",
    # "manim[gui]>=0.20.1",
    "ipython>=9.10.0",
    "manim-slides[pyside6-full]>=5.5.4",
    "manimgl>=1.7.2",
    "pyquaternion>=0.9.9",
    "setuptools>=82.0.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"

[tool.basedpyright]
venvPath = "."
venv = ".venv"


[tool.ruff.lint]
exclude = ["projects"]
extend-ignore = ["F401", "F403"]
